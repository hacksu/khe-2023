name: CI/CD
run-name: Build

on:
  workflow_dispatch:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    concurrency:
      group: build
      cancel-in-progress: true

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16.x
          cache: 'yarn'
      - name: Installing Dependencies
        run: yarn install --frozen-lockfile --prefer-offline
      - name: Building - Server
        run: yarn build --filter=@kenthackenough/server
      - name: Building - Staff
        run: yarn build --filter=@kenthackenough/staff
      - name: Building - Web
        run: yarn build --filter=@kenthackenough/web