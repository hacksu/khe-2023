name: CI/CD

on:
  workflow_dispatch:
  push:

jobs:
  lint:
    name: Lint
    uses: hacksu/khe-2023/.github/workflows/lint.yml@reorg

  build:
    name: Build
    needs: [lint]
    uses: hacksu/khe-2023/.github/workflows/build.yml@reorg

  publish:
    name: Publish
    if: github.ref == 'refs/heads/main' && needs.build.outputs.publish == 'true'
    needs: [build]
    uses: hacksu/khe-2023/.github/workflows/publish.yml@reorg
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

  deploy:
    name: Deploy
    if: github.ref == 'refs/heads/reorg' && (true || github.event_name == 'workflow_dispatch' || contains(github.event.head_commit.message, '@deploy'))
    needs: [build]
    uses: hacksu/khe-2023/.github/workflows/deploy.yml@reorg
    secrets:
      key: ${{ secrets.KHE_DEV_KEY }}
      host: ${{ secrets.KHE_DEV_HOST }}
    with:
      directory: /opt/khe/khe-2023
      
      

  health:
    name: Health
    needs: [build, publish, deploy]
    if: ${{ always() }}
    uses: hacksu/khe-2023/.github/workflows/health.yml@reorg
    secrets:
      key: ${{ secrets.KHE_DEV_KEY }}
      host: ${{ secrets.KHE_DEV_HOST }}