name: CI/CD

on:
  workflow_dispatch:
  push:
    branches:
      - "actions-improvements"

jobs:
  lint:
    name: Lint
    uses: hacksu/khe-2023/.github/workflows/actions/lint.yml@actions-improvements

  build:
    name: Build
    needs: [lint]
    uses: hacksu/khe-2023/.github/workflows/actions/build.yml@actions-improvements